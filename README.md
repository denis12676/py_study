# 🤖 Wildberries AI Agent

ИИ-агент для автоматизации управления магазином на Wildberries через API.

## Возможности

- 📦 **Управление товарами** - получение каталога, обновление цен, проверка остатков
- 📊 **Аналитика** - отчеты о продажах, выручка, топ товаров, детальная статистика
- 📢 **Реклама** - управление кампаниями, ставками, запуск и остановка
- 📦 **Заказы** - мониторинг новых заказов, управление сборкой
- 🔄 **Автоматизация** - ИИ автоматически выбирает нужный метод API

## 🚀 Веб-интерфейс (НОВОЕ!)

Простой и удобный дашборд с графиками:

```bash
pip install -r requirements.txt
streamlit run dashboard.py
```

Или запустите `run_dashboard.bat` на Windows.

Откроется в браузере: http://localhost:8501

### Возможности дашборда:
- 📊 **Главная** - ключевые метрики и быстрые действия
- 💬 **AI Чат** - общайтесь с агентом на русском языке  
- 📦 **Товары** - каталог, поиск, управление ценами
- 📈 **Аналитика** - графики продаж, топ товары, отчеты
- 📢 **Реклама** - управление кампаниями, бюджеты

## Быстрый старт

### 1. Получение API токена

1. Войдите в [личный кабинет продавца](https://seller.wildberries.ru)
2. Перейдите в раздел **Профиль** → **API Интеграции**
3. Нажмите **Создать токен**
4. Выберите категории:
   - ✅ Content (товары и цены)
   - ✅ Statistics (отчеты и аналитика)
   - ✅ Marketplace (заказы)
   - ✅ Promotion (реклама)
5. Сохраните токен (показывается только один раз!)

### 2. Установка

```bash
# Клонируйте репозиторий
cd wildberries-ai-agent

# Установите зависимости (только requests)
pip install requests
```

### 3. Запуск

**Интерактивный режим:**
```bash
python main.py
```

**Программное использование:**
```python
from ai_agent import WildberriesAIAgent

# Инициализация агента
agent = WildberriesAIAgent("ваш_api_токен")

# Выполнение команд на естественном языке
agent.execute("Покажи все мои товары")
agent.execute("Какая выручка за последние 30 дней?")
agent.execute("Топ 10 продаваемых товаров")
agent.execute("Обнови цену товара 12345 на 1500 рублей")
agent.execute("Запусти рекламную кампанию 67890")
```

## Примеры запросов

### Товары и цены
- `"Покажи все мои товары"` - выводит каталог с ценами
- `"Найди товар iPhone 15"` - поиск по названию
- `"Обнови цену 12345 на 1500 рублей"` - изменение цены
- `"Проверь остатки на складах"` - текущие запасы
- `"Товары в карантине"` - не обновлялись >30 дней

### Аналитика
- `"Выручка за 30 дней"` - отчет о продажах
- `"Топ 5 товаров"` - лучшие продажи
- `"Продажи за неделю"` - отчет за 7 дней
- `"Детальный отчет"` - финансы с комиссиями
- `"Остатки на складах WB"` - инвентаризация

### Реклама
- `"Покажи рекламные кампании"` - список всех
- `"Запусти кампанию 12345"` - активация
- `"Останови кампанию 12345"` - пауза
- `"Измени ставку 12345 на 100"` - обновление CPC
- `"Статистика кампании 12345"` - ROI и эффективность

### Заказы
- `"Новые заказы"` - для сборки FBS
- `"Подтверди сборку заказа 12345"` - готов к отгрузке
- `"Отмени заказ 12345"` - отмена

## Архитектура проекта

```
wildberries-ai-agent/
├── wb_client.py          # Базовый HTTP клиент для API
├── api_registry.py       # Реестр всех методов API (60+ методов)
├── managers.py           # Удобные классы: ProductsManager, AnalyticsManager...
├── ai_agent.py          # ИИ-агент с автовыбором методов
├── main.py              # Точка входа (CLI интерфейс)
└── README.md            # Документация
```

### Как работает автовыбор методов?

Агент анализирует запрос пользователя и автоматически:
1. Определяет тип задачи (товары, аналитика, реклама...)
2. Извлекает параметры (артикулы, цены, даты...)
3. Выбирает нужный метод из `api_registry.py`
4. Выполняет запрос к API
5. Форматирует результат для удобного чтения

### Прямое использование менеджеров

Если нужен полный контроль, используйте менеджеры напрямую:

```python
from wb_client import WildberriesAPI, WBConfig
from managers import ProductsManager, AnalyticsManager, AdvertisingManager

# Инициализация
config = WBConfig(api_token="ваш_токен")
api = WildberriesAPI(config)

# Работа с товарами
products = ProductsManager(api)
all_goods = products.get_all_products()
products.update_price(nm_id=12345, price=1500, discount=10)

# Аналитика
analytics = AnalyticsManager(api)
sales = analytics.get_sales(days=30)
revenue = analytics.calculate_revenue(days=7)
top = analytics.get_top_products(limit=10)

# Реклама
adv = AdvertisingManager(api)
campaigns = adv.get_campaigns()
adv.start_campaign(12345)
adv.update_bid([12345], 150)
```

## Доступные методы API

Полный список методов в `api_registry.py`:

- **Content** - 15 методов (товары, цены, остатки, категории)
- **Statistics** - 10 методов (продажи, заказы, отчеты)
- **Marketplace** - 8 методов (заказы FBS/DBS)
- **Promotion** - 10 методов (реклама, кампании, ставки)
- **Feedbacks** - 5 методов (отзывы, вопросы)
- **Analytics** - 3 метода (поисковые запросы)
- **General** - 3 метода (информация о магазине)

Всего: **60+ методов API**

## Лимиты API

Wildberries имеет ограничения на количество запросов:

- **Content** - 100 запросов/мин
- **Statistics** - 300 запросов/мин
- **Marketplace** - 1000 запросов/мин
- **Promotion** - 5 запросов/сек

Агент автоматически учитывает эти ограничения.

## Безопасность

- ✅ Токен хранится только в памяти
- ✅ Никакие данные не логируются
- ✅ Все запросы идут через HTTPS
- ✅ Рекомендуется использовать ограниченные токены (только нужные категории)

## Расширение функционала

Добавить новый метод просто:

1. Добавьте описание в `api_registry.py`:
```python
{
    "name": "my_new_method",
    "description": "Описание что делает",
    "endpoint": "/api/v1/endpoint",
    "method": "GET",
    "category": "statistics",
    "params": {...},
    "use_cases": ["Сценарий 1", "Сценарий 2"]
}
```

2. Добавьте обработку в `ai_agent.py`:
```python
elif action == "my_new_method":
    return self.some_manager.some_method()
```

3. Готово! Агент автоматически распознает запросы

## 🧪 Тестирование

Проект включает полный набор автоматических тестов для предотвращения регрессий.

### Установка зависимостей для тестов

```bash
pip install -r requirements-dev.txt
```

### Запуск тестов

```bash
# Все тесты
pytest

# С подробным выводом
pytest -v

# С покрытием кода
pytest --cov=. --cov-report=html

# Только unit-тесты (быстро)
pytest -m unit

# Конкретный модуль
pytest tests/test_managers.py -v

# Или используйте скрипт
run_tests.bat all
run_tests.bat unit
run_tests.bat coverage
```

### Структура тестов

```
tests/
├── test_wb_client.py    # Тесты API клиента (rate limiting, retries)
├── test_managers.py     # Тесты бизнес-логики (расчет выручки)
├── test_ai_agent.py      # Тесты AI (распознавание запросов)
└── test_dashboard.py     # Тесты UI
```

### Ключевые тесты

- **test_calculate_revenue_uses_forpay** - проверяет что выручка считается правильно
- **test_429_rate_limit_retry** - проверяет обработку rate limiting
- **TestRequestAnalysis** - проверяет распознавание запросов на русском

### CI/CD

Автоматические тесты запускаются при каждом push:
- Python 3.9, 3.10, 3.11, 3.12
- Проверка покрытия кода
- Lint с flake8

## Поддержка

Документация WB API: https://dev.wildberries.ru/

## Лицензия

MIT License - свободное использование для любых целей.

---

**Готовы автоматизировать ваш магазин на Wildberries?** 🚀
