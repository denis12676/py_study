# 🔄 Обновление кода после деплоя

После того как приложение задеплоено в Streamlit Cloud, обновление кода делается в 3 простых шага.

## ⚡ Быстрая инструкция

### 1. Внесите изменения в код
Редактируйте файлы локально в папке `wildberries-ai-agent/`.

### 2. Отправьте изменения в GitHub

**Вариант А: Через скрипт (просто)**
```bash
cd wildberries-ai-agent
update_code.bat
```

**Вариант Б: Вручную**
```bash
cd wildberries-ai-agent

# Проверьте что изменилось
git status

# Добавьте изменения
git add -A

# Создайте commit
git commit -m "Описание изменений"

# Отправьте в GitHub
git push origin master
```

### 3. Streamlit Cloud автоматически обновит приложение!

- ⏱️ Обычно происходит в течение **1-2 минут**
- 🔄 Страница приложения перезагрузится автоматически
- 👀 Пользователи увидят новую версию

## 🎯 Что происходит при обновлении

### Локально (ваш компьютер)
```
Вы редактируете код → Git отслеживает изменения
```

### GitHub (репозиторий)
```
git push → Код загружается на GitHub
```

### Streamlit Cloud (облако)
```
GitHub webhook → Streamlit видит изменения
↓
Автоматическая перезагрузка приложения
↓
Пользователи видят обновленную версию
```

## 📝 Примеры обновлений

### Пример 1: Исправили баг
```bash
git add -A
git commit -m "Fix: Revenue calculation error"
git push origin master
```

### Пример 2: Добавили новую функцию
```bash
git add -A
git commit -m "Add: Weekly sales report export"
git push origin master
```

### Пример 3: Обновили дизайн
```bash
git add -A
git commit -m "Update: New dashboard layout"
git push origin master
```

## 🚀 Ручная перезагрузка (если нужно срочно)

Если изменения не появились автоматически:

1. Зайдите в https://share.streamlit.io
2. Найдите ваше приложение
3. Нажмите **⋮** → **Reboot**

Или используйте меню: **Manage app** → **Reboot**

## ⚠️ Важные моменты

### ✅ Что сохраняется при обновлении:
- 🔐 **Secrets** (API токен) - остается в облаке
- 📊 **Данные пользователей** в session_state (пока сессия активна)
- 🗄️ **Кэш** на стороне сервера (если используется)

### ❌ Что сбрасывается при обновлении:
- 🔄 **Session state** - пользователям нужно заново ввести токен (если он не из Secrets)
- 💾 **Локальные изменения** на компьютере пользователя
- ⚡ **Активные соединения** WebSocket

### 🔒 Secrets не трогаем!
**API токен остается в Streamlit Cloud!** 
- Его не нужно обновлять при каждом деплое
- Он хранится отдельно от кода
- Даже при полной перезагрузке приложения токен сохраняется

## 🆘 Возможные проблемы

### Проблема 1: "Updates not showing"
**Решение:**
```bash
# Force redeploy на Streamlit Cloud
# 1. Go to https://share.streamlit.io
# 2. Click ⋮ → Reboot
```

### Проблема 2: "Permission denied" при push
**Решение:**
```bash
# Проверьте удаленный репозиторий
git remote -v

# Если неправильный - исправьте
git remote remove origin
git remote add origin https://github.com/denis12676/wb-ai-agent.git
git push -u origin master
```

### Проблема 3: "rejected - non-fast-forward"
**Решение:**
```bash
# Сначала получите изменения
git pull origin master

# Разрешите конфликты если есть
# Затем отправьте
git push origin master
```

## 📋 Чеклист обновления

- [ ] Внесены изменения в код
- [ ] Проверено локально (streamlit run dashboard.py)
- [ ] Сделан commit с понятным сообщением
- [ ] Отправлено на GitHub (git push)
- [ ] Подождено 1-2 минуты
- [ ] Проверено в облаке
- [ ] При необходимости - Reboot через интерфейс

## 💡 Советы

### 1. Частые мелкие коммиты
```bash
# Хорошо: маленькие изменения
git commit -m "Fix typo in dashboard"
git commit -m "Add loading spinner"

# Плохо: огромный коммит с кучей изменений
git commit -m "Many updates"
```

### 2. Проверяйте перед push
```bash
# Всегда проверяйте что отправляете
git diff --cached
```

### 3. Используйте .gitignore
```bash
# Убедитесь что чувствительные файлы не отправятся
git status
# .env должен быть красным (не отслеживается)
```

## 🎉 Итог

Обновление кода в Streamlit Cloud - это просто:
1. **Изменить** код локально
2. **Закоммитить** изменения
3. **Отправить** на GitHub

Всё остальное Streamlit делает автоматически! 🚀

**Ваш API токен всегда в безопасности** - он остается в Secrets и не меняется при обновлениях кода.
