# 🚀 Деплой в Streamlit Community Cloud

## Безопасное хранение API ключа

API ключ Wildberries **НИКОГДА** не попадет в публичный доступ при правильной настройке!

## 📋 Пошаговая инструкция

### 1. Подготовка репозитория

Убедитесь, что в `.gitignore` есть:
```
.env
.streamlit/secrets.toml
```

**Важно:** Файл `.streamlit/secrets.toml` уже добавлен в `.gitignore`, поэтому не попадет в Git!

### 2. Создание репозитория на GitHub

```bash
# Инициализируйте Git (если еще не сделано)
git init

# Добавьте все файлы КРОМЕ секретов
git add .
git commit -m "Initial commit"

# Создайте репозиторий на GitHub и отправьте код
git remote add origin https://github.com/username/wb-ai-agent.git
git push -u origin main
```

### 3. Деплой в Streamlit Cloud

1. Перейдите на https://streamlit.io/cloud
2. Нажмите **"New app"**
3. Выберите ваш GitHub репозиторий
4. Укажите путь к файлу: `dashboard.py`
5. Нажмите **"Deploy"**

### 4. 🔐 Настройка Secrets (КРИТИЧЕСКИ ВАЖНО!)

После деплоя:

1. Откройте **Settings** → **Secrets**
2. Добавьте переменную:
   - **Key**: `WB_API_TOKEN`
   - **Value**: `eyJhbGciOiJFUzI1Ni...` (ваш реальный токен)
3. Нажмите **Save**

**Важно:** 
- Токен хранится в зашифрованном виде на серверах Streamlit
- Даже разработчики Streamlit не имеют доступа к вашему токену
- Токен не отображается в логах и не попадает в код

### 5. Перезапуск приложения

Нажмите **"Reboot"** после добавления secrets.

## 🔒 Как это работает

### Локальная разработка:
```
.env файл → API ключ загружается локально
```

### Streamlit Cloud:
```
Streamlit Secrets → Зашифрованное хранилище → API ключ загружается в runtime
```

## 🛡️ Безопасность

### Что защищено:
- ✅ API ключ не в коде
- ✅ API ключ не в Git истории
- ✅ API ключ не виден в логах
- ✅ API ключ не доступен другим пользователям
- ✅ API ключ шифруется на сервере Streamlit

### Что видят пользователи:
- 👁️ Интерфейс дашборда
- 👁️ Данные их магазина
- 🚫 **НЕ видят API ключ**

## 📁 Структура файлов

```
wildberries-ai-agent/
├── .streamlit/
│   └── secrets.toml.template    # Шаблон (без реального токена!)
├── .gitignore                    # Исключает .env и secrets.toml
├── dashboard.py                  # Главный файл
├── requirements.txt              # Зависимости
└── README.md                     # Документация
```

## 🚨 Важные замечания

### НЕ ДЕЛАЙТЕ ЭТОГО:

❌ **НЕ** коммитьте `.env` файл
❌ **НЕ** коммитьте `secrets.toml` с реальным токеном
❌ **НЕ** пишите токен в коде
❌ **НЕ** отправляйте токен в чатах

### ПРАВИЛЬНО:

✅ Используйте `.env` только локально
✅ Используйте Streamlit Secrets для облака
✅ Добавьте `.env` и `secrets.toml` в `.gitignore`
✅ Используйте `secrets.toml.template` как пример

## 🔄 Обновление токена

Если токен скомпрометирован:

1. Отзовите токен в личном кабинете WB
2. Создайте новый токен
3. Обновите в Streamlit Secrets:
   - Settings → Secrets → Edit → Save
4. Перезагрузите приложение

## 🐛 Отладка

### Приложение не видит токен:

Проверьте логи в Streamlit Cloud:
```python
# Добавьте временно в dashboard.py
if "WB_API_TOKEN" in st.secrets:
    st.sidebar.success("Secrets загружены")
else:
    st.sidebar.error("Secrets НЕ загружены")
```

### Ошибка "Module not found":

Убедитесь, что `requirements.txt` содержит все зависимости:
```
streamlit>=1.28.0
pandas>=2.0.0
plotly>=5.18.0
requests>=2.28.0
```

## 📞 Поддержка

- Streamlit Cloud Docs: https://docs.streamlit.io/streamlit-community-cloud
- Secrets management: https://docs.streamlit.io/streamlit-community-cloud/get-started/deploy-an-app/connect-to-data-sources/secrets-management

## 🎉 Готово!

После деплоя ваш дашборд будет доступен по адресу:
```
https://username-wb-ai-agent.streamlit.app
```

**API ключ полностью защищен!** 🔒
